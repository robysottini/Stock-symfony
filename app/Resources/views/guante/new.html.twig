{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo guante</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn " />
    {{ form_end(form) }}
    </br>
    </br>
    </br>

    <a href="{{ path('guante_index') }}" class="btn waves-effect waves-light">Volver a la lista</a>
{% endblock %}
