{% extends 'base.html.twig' %}

{% block body %}
    <h1>PrincipioActivo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ principioActivo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ principioActivo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('principioactivo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('principioactivo_edit', { 'id': principioActivo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
