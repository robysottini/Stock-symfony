{% extends 'base.html.twig' %}

{% block body %}
    <h1>Marcas</h1>

    <table class="bordered striped highlight responsive-table">
        <thead>
            <tr>
                <th>Nombre</th>
            </tr>
        </thead>
        <tbody>
        {% for marca in marcas %}
            <tr>
                <td>{{ marca.nombre }}</td>
                <td>
                    <a href="{{ path('marca_edit', { 'id': marca.id }) }}" class="btn waves-effect waves-light">Editar</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('marca_new') }}" class="btn waves-effect waves-light">Nueva marca</a>
{% endblock %}
