{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nueva marca</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn btn-primary" />
    {{ form_end(form) }}

    <a href="{{ path('marca_index') }}" class="btn btn-primary">Volver a la lista</a>
{% endblock %}
