{% extends 'base.html.twig' %}

{% block body %}
    <h1>Marca</h1>

    <table class="bordered striped highlight responsive-table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ marca.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marca_index') }}" class="btn waves-effect waves-light">Volver a la lista</a></br></br>
    <a href="{{ path('marca_edit', { 'id': marca.id }) }}" class="btn waves-effect waves-light">Editar</a></br></br>
    {{ form_start(delete_form) }}
        <input type="submit" value="Eliminar" class="btn">
    {{ form_end(delete_form) }}
{% endblock %}
